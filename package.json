{
  "name": "msp-portal",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run prepare && astro dev",
    "prepare": "node scripts/sync-sources.mjs && node scripts/build-docs.mjs",
    "build": "npm run prepare && astro build",
    "preview": "astro preview",
    "check": "npm run prepare && astro check",
    "sync": "node scripts/sync-sources.mjs",
    "validate:sources": "node scripts/validate-sources.mjs"
  },
  "dependencies": {
    "astro": "^4.16.18",
    "marked": "^13.0.3",
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@types/node": "^26.4.1",
    "typescript": "^5.9.3"
  }
}
